[Français](README.md) | [English](README.en.md) | [العربية](README.ar.md)

# تطبيق Groq للتحويل من نص إلى صوت (TTS)

> **هدف المشروع**: توفير تطبيق صغير بواجهة رسومية (Tkinter) يحول النص إلى صوت باستخدام **واجهة Groq TTS**.
>
> هذا الدليل **مفصل وبأسلوب مبسط** لمساعدة المبتدئين على فهم الخطوات.

---

## 📚 المحتويات

- [✅ ماذا يفعل البرنامج (شرح مبسط)](#-ماذا-يفعل-البرنامج-شرح-مبسط)
- [🧩 المتطلبات (ما يلزم قبل البدء)](#-المتطلبات-ما-يلزم-قبل-البدء)
- [📁 الملفات المهمة في المشروع](#-الملفات-المهمة-في-المشروع)
- [🧪 التثبيت خطوة بخطوة (للمبتدئين)](#-التثبيت-خطوة-بخطوة-للمبتدئين)
- [▶️ تشغيل البرنامج](#️-تشغيل-البرنامج)
- [🧭 استخدام البرنامج (شرح مفصل)](#-استخدام-البرنامج-شرح-مفصل)
- [🧠 كيف يعمل البرنامج (نسخة مبسطة)](#-كيف-يعمل-البرنامج-نسخة-مبسطة)
- [🔧 حل المشكلات (أخطاء شائعة)](#-حل-المشكلات-أخطاء-شائعة)
- [📌 نصائح الاستخدام](#-نصائح-الاستخدام)
- [📄 الرخصة](#-الرخصة)
- [✅ ملخص سريع جدًا](#-ملخص-سريع-جدًا)

---

## ✅ ماذا يفعل البرنامج (شرح مبسط)

البرنامج يقوم بـ:

1. **التحقق** من توفر مكتبات بايثون المطلوبة.
2. **طلب** مفتاح Groq API (ويحفظه في `api_key.txt`).
3. **جلب** قائمة نماذج TTS المتاحة في Groq.
4. **تمكين المستخدم** من اختيار النموذج، والصوت، والسرعة.
5. **تحويل النص إلى صوت** ثم:
   - **تشغيل** الصوت مباشرة، أو
   - **حفظه** في ملف `.wav`.

---

## 🧩 المتطلبات (ما يلزم قبل البدء)

### 1) جهاز **Windows**
يستخدم البرنامج `winsound` و `os.startfile` وهما **خاصان بنظام Windows**.

### 2) تثبيت Python
- الإصدار الموصى به: **Python 3.10+**
- تحقق من الإصدار عبر الطرفية:
  ```bash
  python --version
  ```

### 3) مفتاح Groq API
أنشئ حسابًا في Groq واحصل على مفتاح API الخاص بك.

---

## 📁 الملفات المهمة في المشروع

- `tts.py`: البرنامج الرئيسي.
- `README.md`: الدليل بالفرنسية.
- `README.en.md`: الدليل بالإنجليزية.
- `README.ar.md`: الدليل بالعربية.
- `api_key.txt`: يُنشأ تلقائيًا لتخزين المفتاح محليًا.
- `logd.txt`: ملف السجلات (يُنشأ تلقائيًا).

---

## 🧪 التثبيت خطوة بخطوة (للمبتدئين)

### ✅ الخطوة 1 — فتح الطرفية
على Windows:
- **Command Prompt** (`cmd`)
- أو **PowerShell**

### ✅ الخطوة 2 — الانتقال إلى مجلد المشروع
مثال:
```bash
cd C:\path\to\the\project
```

### ✅ الخطوة 3 — (اختياري ومُستحسن) إنشاء بيئة افتراضية
يساعد على عدم خلط المكتبات بين المشاريع.

```bash
python -m venv .venv
```

تفعيل البيئة:
```bash
.venv\Scripts\activate
```

سترى `(.venv)` في بداية سطر الطرفية.

### ✅ الخطوة 4 — تثبيت المتطلبات
البرنامج يحتاج إلى:
- `groq`
- `requests`

ثبّتها باستخدام:
```bash
pip install groq requests
```

---

## ▶️ تشغيل البرنامج

من داخل مجلد المشروع:

```bash
python tts.py
```

ستظهر نافذة رسومية.

---

## 🧭 استخدام البرنامج (شرح مفصل)

### 1) التحقق من المكتبات
عند بدء التشغيل، تظهر نافذة تتحقق من وجود `groq` و `requests`.

- إذا كان كل شيء جاهزًا: تظهر رسالة خضراء ✅
- إذا كان هناك نقص: ستظهر أوامر `pip install ...` لنسخها

انقر **إعادة التحقق** بعد التثبيت.

---

### 2) إدخال مفتاح API
تظهر نافذة ثانية تطلب **مفتاح Groq API**.

- يتم **حفظه محليًا** في `api_key.txt`.
- لن تحتاج لإدخاله كل مرة.

---

### 3) اختيار النموذج والصوت والسرعة
في النافذة الرئيسية:

- **نموذج TTS**: قائمة النماذج المتاحة في حسابك.
- **الصوت**: يعتمد على النموذج المختار.
- **السرعة**: بين **0.5x** (بطيء) و **3.0x** (سريع جدًا).

---

### 4) كتابة النص
في مربع النص الكبير، اكتب ما تريد سماعه.

مثال:
```
مرحبًا، هذا اختبار للتحويل من نص إلى صوت.
```

---

### 5) التشغيل أو الحفظ
يوجد زران:

- **تشغيل**: يحول النص إلى صوت ويشغله.
- **حفظ**: يحفظ الصوت في ملف `.wav`.

---

## 🧠 كيف يعمل البرنامج (نسخة مبسطة)

### 1) يطلب التطبيق نماذج Groq
واجهة Groq تعيد قائمة النماذج المتاحة.

### 2) يرشّح نماذج TTS
يحتفظ بالنماذج التي تحتوي كلمات مثل `tts` أو `speech` أو `orpheus`.

### 3) عند الضغط على تشغيل أو حفظ
يرسل طلب HTTP إلى:

```
https://api.groq.com/openai/v1/audio/speech
```

مع:
- النص
- النموذج
- الصوت
- السرعة

وترجع Groq ملف **WAV**، ويتم تشغيله أو حفظه.

---

## 🔧 حل المشكلات (أخطاء شائعة)

### ❌ "الوحدة غير موجودة"
إذا ظهر خطأ حول `groq` أو `requests`:
```bash
pip install groq requests
```

### ❌ خطأ في مفتاح API
- تأكد من صحة المفتاح.
- يمكنك حذف `api_key.txt` لإعادة إدخاله.

### ❌ لا يوجد صوت
- تحقق من تشغيل السماعات.
- تأكد أن Windows يستطيع تشغيل ملفات `.wav`.

---

## 📌 نصائح الاستخدام

- التزم بنظام **Windows** لتجنب مشاكل التوافق.
- استخدم نصًا قصيرًا عند الاختبار لأول مرة.
- لتغيير مفتاح API:
  - احذف `api_key.txt`
  - أعد تشغيل البرنامج

---

## 📄 الرخصة

هذا المشروع تحت رخصة MIT. راجع [LICENSE](LICENSE) للنص الكامل.

---

## ✅ ملخص سريع جدًا

1. ثبّت Python
2. ثبّت `groq` و `requests`
3. شغّل `python tts.py`
4. أدخل مفتاح Groq
5. اكتب النص → تشغيل أو حفظ

---

إذا كنت مبتدئًا في البرمجة، اتبع هذا الدليل خطوة بخطوة وستنجح. 🎉
